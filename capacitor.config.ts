import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ryby.workouttracker',
  appName: 'workout-tracker',
  webDir: 'dist'
};

export default config;
